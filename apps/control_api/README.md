# Lira Control API

FastAPI do Control Center (chat, mídia, WebSocket, gerenciamento Discord/WhatsApp).

```bash
python apps/control_api/main.py
```

Porta padrão: **8042**. Shim legado: `src/api/server.py`.